# form-renderer

Shared workspace package that renders a form-spec JSON document as a React UI.

Consumed by both `form-web` (public renderer) and `form-admin` (live builder preview). Will also be bundled inside `@supanut9/form-sdk` for inline-embed mode (Wave 5 / L14).

Wave 3 / L8 owns the full implementation.
